import ApplicationsContainer from "../components/applications-container";

const Index = () => {
	return (
		<main className="gradient-bg flex flex-col h-full p-3 gap-3 overflow-auto pb-16">
			<ApplicationsContainer />
		</main>
	);
};

export default Index;
